% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{plotVolcano.label}
\alias{plotVolcano.label}
\title{Create a static labeled Volcano plot}
\usage{
plotVolcano.label(
  res,
  fdr.thres = 0.1,
  fc.thres = 0,
  neg_log_padj.lim = NULL,
  fc.lim = c(-4, 4),
  lab.genes = NULL,
  tolower.cols = c("SYMBOL", "ALIAS"),
  color_by = c("baseMean", "significance"),
  colorscale = "viridis",
  alpha = 0.6
)
}
\arguments{
\item{res}{data.frame with DE analysis results. Must contain
"padj", "baseMean" & "log2FoldChange" columns}

\item{fdr.thres}{False discovery rate (FDR) threshold}

\item{fc.thres}{log2FoldChange threshold}

\item{neg_log_padj.lim}{y-axis limits}

\item{fc.lim}{x-axis limits}

\item{lab.genes}{genes to label on plot}

\item{tolower.cols}{column names that will be converted to lower case}

\item{color_by}{variable to color points by ('baseMean' or 'significance')}

\item{colorscale}{ggplot-compatible continuous colorscale name
(viridis option), only used when color_by='baseMean'}

\item{alpha}{point opacity between 0 and 1}
}
\value{
ggplot handle
}
\description{
This function creates a static ggplot Volcano plot from a data.frame
containing DE analysis results. Mirrors plotVolcano.label_ly but
renders with ggplot2 for use in downloads.
}
\examples{
n_genes <- 100
res <- data.frame(
         baseMean = runif(n_genes, 10, 1000),
         log2FoldChange = rnorm(n_genes, 0, 2),
         lfcSE = runif(n_genes, 0.1, 0.5),
         stat = rnorm(n_genes, 0, 3),
         pvalue = runif(n_genes, 0, 1),
         padj = runif(n_genes, 0, 1),
         symbol = paste0("GENE", 1:n_genes),
         row.names = paste0("gene", 1:n_genes)
       )

plotVolcano.label(res, lab.genes = c("gene1", "gene2"))

}
