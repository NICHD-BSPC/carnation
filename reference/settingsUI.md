# Settings module ui

This generates the settings tab that allows users to add data areas and
app user/user groups.

## Usage

``` r
settingsUI(id, panel, username)
```

## Arguments

- id:

  Input id

- panel:

  context for generating ui elements ('sidebar' or 'main')

- username:

  user name
