# Metadata module ui

This generates the metadata tab that allows users to view the metadata
attached to the objects being viewed.

## Usage

``` r
metadataUI(id, panel)
```

## Arguments

- id:

  Input id

- panel:

  context for generating ui elements ('sidebar' or 'main')
