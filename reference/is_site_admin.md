# is user an admin?

is user an admin?

## Usage

``` r
is_site_admin(u)
```

## Arguments

- u:

  username

## Value

boolean to indicate is user is in admin group

## Examples

``` r
# check if default user is admin
yy <- is_site_admin(u='admin')
```
