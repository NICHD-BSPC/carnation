# Cnetplot server function

Cnetplot server function

## Usage

``` r
cnetPlotServer(id, obj, config)
```

## Arguments

- id:

  ID string used to match the ID used to call the module UI function

- obj:

  reactive, dataframe with enrichment results

- config:

  reactive list with config settings
